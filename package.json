{
  "name": "portus",
  "version": "0.1.14",
  "description": "PORTUS — Desktop app para leitura de equipamentos via porta serial, vinculada a receitas e lotes.",
  "main": "dist/main/index.js",
  "author": "",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "dev": "npm run build:main && concurrently --raw -n MAIN,RENDERER,ELECTRON -c blue,green,yellow \"tsc -p tsconfig.main.json --watch --preserveWatchOutput\" \"vite --configLoader runner\" \"npm run dev:electron\"",
    "dev:electron": "wait-on http://localhost:5173 && cross-env ELECTRON_DEV=1 electron .",
    "electron": "cross-env ELECTRON_DEV=1 electron .",
    "build": "npm run build:renderer && npm run build:main",
    "build:main": "tsc -p tsconfig.main.json",
    "build:renderer": "vite build --configLoader runner",
    "start": "cross-env NODE_ENV=production ELECTRON_DEV=1 electron .",
    "prepackage": "npm run validate:release && npm version patch --no-git-tag-version",
    "package": "npm run build && electron-builder && node scripts/copy-installer-to-root.mjs",
    "rebuild": "electron-rebuild -f -w serialport",
    "typecheck": "tsc -p tsconfig.main.json --noEmit && tsc -p tsconfig.renderer.json --noEmit",
    "sim:build": "tsc -p tsconfig.tools.json",
    "sim": "npm run sim:build && node dist/tools/serial-sim.js",
    "sim:test": "npm run sim:build && node dist/tools/sim-test.js",
    "test:postgres:concurrency": "node database/tests/003_concurrent_closure.mjs",
    "package:database-installer": "node scripts/package-database-installer.mjs",
    "validate:release": "node scripts/validate-release-readiness.mjs",
    "test": "vitest run --config vitest.config.ts --configLoader runner"
  },
  "dependencies": {
    "@fontsource/dm-sans": "^5.2.8",
    "@fontsource/ibm-plex-mono": "^5.2.7",
    "@fontsource/syne": "^5.2.7",
    "bcryptjs": "^2.4.3",
    "framer-motion": "^12.38.0",
    "jsbarcode": "^3.12.3",
    "lucide-react": "^1.16.0",
    "pg": "^8.16.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "serialport": "^12.0.0",
    "sql.js": "^1.14.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@electron/rebuild": "^4.2.0",
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^22.7.4",
    "@types/pg": "^8.15.5",
    "@types/react": "^18.3.11",
    "@types/react-dom": "^18.3.0",
    "@types/sql.js": "^1.4.11",
    "@vitejs/plugin-react": "^6.1.1",
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0",
    "electron": "^44.3.0",
    "electron-builder": "^26.15.3",
    "playwright-core": "^1.60.0",
    "typescript": "^5.6.2",
    "vite": "^8.2.2",
    "vitest": "^5.0.0",
    "wait-on": "^9.0.10"
  },
  "build": {
    "appId": "com.portus.app",
    "productName": "Portus",
    "icon": "build/icon.png",
    "files": [
      "dist/**/*",
      "package.json"
    ],
    "directories": {
      "output": "release",
      "buildResources": "build"
    },
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ],
      "icon": "build/icon.ico",
      "artifactName": "PORTUS-Setup-${version}-${arch}.${ext}",
      "certificateFile": null,
      "certificatePassword": null,
      "signingHashAlgorithms": [
        "sha256"
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "Portus",
      "installerIcon": "build/icon.ico",
      "uninstallerIcon": "build/icon.ico",
      "installerHeaderIcon": "build/icon.ico"
    }
  }
}
